{{ diagnosis_name }}:
{% for protocol_id, details in protocols.items() %}
  {{ protocol_id }}:
    target: "{{ details.target }}"
    pattern: "{{ details.pattern }}"
    frequency_hz: {{ details.frequency_hz_yaml }}
    {% if details.intensity_pct_mt_yaml != 'null' %}
    intensity_pct_mt: {{ details.intensity_pct_mt_yaml }}
    {% elif details.intensity_pct_amt_yaml != 'null' %}
    intensity_pct_amt: {{ details.intensity_pct_amt_yaml }}
    {% endif %}
    pulses: {{ details.pulses_yaml }}
    train_s: {{ details.train_s_yaml }}
    iti_s: {{ details.iti_s_yaml }}
    sessions: "{{ details.sessions }}"
    schedule: "{{ details.schedule }}"
    evidence_level: "{{ details.evidence_level }}"
    evidence_refs:
{% for ref in details.evidence_refs %}
      - "{{ ref }}"
{% endfor %}
{% if not loop.last %}

{% endif %}
{% endfor %}
